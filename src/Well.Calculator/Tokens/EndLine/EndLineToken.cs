using Well.Calculator.Tokens.Base;

namespace Well.Calculator.Tokens.EndLine
{
    public class EndLineToken : BaseToken
    {
        public EndLineToken(string tokenString)
            : base(tokenString)
        {
        }
    }
}